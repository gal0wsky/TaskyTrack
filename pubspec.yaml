name: tasky_track
description: Simple task tracking app built with Flutter.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.2+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.2
  flutter_secure_storage: ^8.0.0
  freezed_annotation: ^2.2.0
  get: ^4.6.5
  get_storage: ^2.1.1
  hooks_riverpod: ^2.3.4
  introduction_screen: ^3.1.7
  material_design_icons_flutter: ^6.0.7096
  riverpod: ^2.3.2

dev_dependencies:
  auto_route_generator: ^5.0.3
  build_runner: ^2.3.3
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.3.2
  lint: ^2.0.1
  mockito: ^5.3.2

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/add-task.gif
    - assets/reorder-tasks.gif
    - assets/delete-task.gif
